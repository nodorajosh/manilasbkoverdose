import { Provider } from "@/components/providers";

import Main from "./main";

export default function TicketsPage() {

    return (
        <Provider>
            <Main />
        </Provider>
    );
}